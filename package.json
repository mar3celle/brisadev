{
  "name": "brisadev",
  "version": "2.0.0",
  "description": "BrisaDev — Netlify Functions + Supabase + Stripe",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'static'",
    "db:seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.44.4",
    "bcrypt": "^5.1.1",
    "nodemailer": "^6.9.13",
    "stripe": "^15.10.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
